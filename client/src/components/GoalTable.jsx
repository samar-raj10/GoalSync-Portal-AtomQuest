import { statusColor } from '../utils/validation';
import ProgressBar from './ProgressBar';
export default function GoalTable({ goals, updates = [], actions }) {
  return <div className="overflow-x-auto rounded border border-gray-200 bg-white"><table className="min-w-full text-sm"><thead className="bg-gray-50 text-left text-xs uppercase text-gray-500"><tr><th className="p-3">Employee</th><th className="p-3">Goal</th><th className="p-3">Target</th><th className="p-3">Weight</th><th className="p-3">Status</th><th className="p-3">Progress</th><th className="p-3">Actions</th></tr></thead><tbody>{goals.map(goal => { const update = updates.find(u => String(u.goal?._id || u.goal) === String(goal._id)); return <tr className="border-t" key={goal._id}><td className="p-3">{goal.employee?.name || 'Me'}</td><td className="p-3"><p className="font-medium">{goal.title}</p><p className="text-xs text-gray-500">{goal.thrustArea}</p>{goal.sharedGoal && <span className="badge bg-blue-100 text-blue-800">Shared</span>}</td><td className="p-3">{goal.target}</td><td className="p-3">{goal.weightage}%</td><td className="p-3"><span className={`badge ${statusColor(goal.status)}`}>{goal.status}</span>{goal.locked && <span className="badge ml-1 bg-gray-900 text-white">Locked</span>}</td><td className="p-3 w-40"><ProgressBar value={update?.progressScore || 0} /></td><td className="p-3">{actions?.(goal)}</td></tr>; })}</tbody></table></div>;
}
